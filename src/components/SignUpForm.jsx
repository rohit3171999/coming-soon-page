/*
* Description: A form to capture visitors’ email addresses.
Purpose: Build a pre-launch email list of potential users.
* */

export default function SignUpForm(){
    return (
        <>
            {/*<form> → Form wrapper*/}

            {/*    <input type="email"> → Email input field*/}

            {/*        <button type="submit"> → “Notify Me” button*/}

            {/*            <p> → Confirmation message (conditionally rendered after submit)*/}
        </>
    );
}